import checkForCaching from '../utilities/caching'

describe('Test if Image pre-stored', () => {
  it('expect checkForCaching function to be defined ', () => {
    expect(checkForCaching).toBeDefined()
  })
})
